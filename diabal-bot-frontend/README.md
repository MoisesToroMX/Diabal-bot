# Diabal Frontend

React/Vite UI for the Excel-to-JSON chat flow.

Run locally:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```

The app uploads CSV/XLSX files to the backend, shows detected JSON field
mapping, supports chat edits and downloads the final JSON.

See the root `README.md` for Docker and full project setup.
